# Version 0.0.3 — Linux production install

## Status

PLANNED

## Goal

```text
Linux package with install.sh
Static files installed under /opt/spaghettichef-angular
Optional nginx config
Optional systemd service
```